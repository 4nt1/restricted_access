RestrictedAccess.configure do |config|

  config.access = [<% @levels.each_with_index do |level, index| %>
                    { level:  :<%= level %>,
                      label:  '',
                      power:  <%= index %> }
                  <%- end %>

  config.resource = :<%= @resource_name %>

<% if @controller_scope %>
  config.controller_scope = :<%= @controller_scope %>
<% else %>
config.controller_scope = nil
<%- end %>

end